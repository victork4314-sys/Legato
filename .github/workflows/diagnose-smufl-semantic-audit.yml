name: Diagnose expanded SMuFL semantic audit
on:
  pull_request:
    branches: [main]
    paths:
      - tools/audit_corrected_catalog_v2.py
      - tools/correct_smufl_semantics_v4.py
      - .github/workflows/diagnose-smufl-semantic-audit.yml
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/smufl-semantic-audit-corrections
      - name: Apply expanded catalog correction
        run: python3 tools/correct_smufl_semantics_v4.py
      - name: Run expanded named audit and capture result
        run: |
          set +e
          python3 tools/audit_corrected_catalog_v2.py > tools/corrected-catalog-audit-v2.txt 2>&1
          CODE=$?
          cat tools/corrected-catalog-audit-v2.txt
          if [ "$CODE" -ne 0 ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add tools/corrected-catalog-audit-v2.txt
            git commit -m "Capture expanded corrected catalog audit failure" || true
            git push origin HEAD:fix/smufl-semantic-audit-corrections
          fi
          exit "$CODE"
