===== toggleTie =====
1514:      this.toggleTie(); msg = 'Tie toggled';
3116:  toggleTie() {
3615:    if (k === 't' || k === 'T') return this.toggleTie();

===== focusList =====
1892:        const list = this.focusList(), it = list[s.focus];
3452:      this.focusList().forEach((item, focus) => targets.push({ zone: zone, mode: mode == null ? s.mode : mode, focus: focus, label: item.label }));
3470:  focusList() {
3522:    const list = this.focusList(), it = list[this.state.focus];

===== activateFocus =====
1867:      if (action === 'confirm') return this.activateFocus();
3519:  activateFocus() {

===== componentDidUpdate =====
1312:  componentDidUpdate() {
2381:  componentDidUpdate() {

===== data-score-caret =====
349:            <div data-score-caret="true" style="{{ caretStyle }}"></div>
2392:      const caret = document.querySelector('[data-score-caret="true"]');

===== tieStyle =====
335:                <div style="{{ n.tieStyle }}"></div>
4548:          if (st.hidden) return { wrapStyle: 'display:none;', glyphStyle: 'display:none;', chordHeads: [], marks: [], glyph: '', flag: '', flagStyle: 'display:none;', stemStyle: 'display:none;', dotStyle: 'display:none;', dotGlyph: '', tupStyle: 'display:none;', tup: '', tieStyle: 'display:none;', selStyle: 'display:none;', acc: '', accStyle: 'display:none;', art: '', artStyle: 'display:none;', orn: '', ornStyle: 'display:none;', dyn: '', dynStyle: 'display:none;', hitStyle: 'display:none;', ptr: '', onSelect: () => {} };
4599:            tieStyle: nextTie ? 'position:absolute;left:4px;top:' + (up ? 8 : -14) + 'px;width:' + Math.max(14, (nextTie.p - n.p) * XS - 12) + 'px;height:11px;border:2px solid var(--ink);border-color:' + (up ? 'transparent transparent var(--ink) transparent' : 'var(--ink) transparent transparent transparent') + ';border-radius:' + (up ? '0 0 50% 50%' : '50% 50% 0 0') + ';' : 'display:none;',

===== autoRests: =====
4112:      autoRests: (() => {

===== staffHits: =====
4167:      staffHits: STAVES.map((st, i) => ({

===== timeTopStyle: =====
3857:          timeTopStyle: 'position:absolute;left:' + timeX + 'px;top:-3px;width:' + timeWidth + 'px;height:27px;display:grid;place-items:center;' + BR + 'font-size:43px;line-height:1;color:var(--ink);white-space:nowrap;cursor:pointer;z-index:5;',

===== setupRows: =====
3907:      setupRows: [

===== themeRows: =====
3727:      themeRows: THEMES.map(t => ({

